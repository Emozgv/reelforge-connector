{
  "name": "reelforge-connector",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "npm run prepare:runtime && tauri build",
    "prepare:runtime": "node scripts/prepare-bundled-runtime.mjs",
    "tauri": "tauri"
  },
  "dependencies": {
    "playwright": "1.55.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
